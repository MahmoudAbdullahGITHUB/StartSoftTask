import 'package:flutter/material.dart';

class ShoppingScreen extends StatelessWidget {
  static const routName = '/shopping-screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Shopping Screen'),
      ),
    );
  }
}
