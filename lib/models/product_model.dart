class ProductModel {
  String productId;
  String title;
  String price;
  String description;
  String category;
  String image;
  String color;

  ProductModel(this.productId, this.title, this.price, this.description, this.category,
      this.image,this.color);
}
